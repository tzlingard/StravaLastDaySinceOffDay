{
  "name": "stravalastoffday",
  "version": "1.0.0",
  "description": "A Strava application to keep track of the number of days since your last day off from running.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "buildStravaApi": "cd strava-api-v3 && npm install && npm link && cd .. && npm link ./strava-api-v3",
    "build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "v18.12.1"
  },
  "author": "Thomas Lingard",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.1",
    "body-parser": "^1.20.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  }
}
